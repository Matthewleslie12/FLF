import { useState } from "react";
import FiendBios from "../../assets/fiend-bios/fiend-bios.json"

const FiendRevolver = () => {

  const [isActive, setIsActive] = useState();

  if (isActive)
  return (
    <div className="">
      
    </div>
  );
};

export default FiendRevolver;